// Generated by: gen
// TypeWriter: slice
// Directive: +gen on Etappe

package tour

import "errors"

// Sort implementation is a modification of http://golang.org/pkg/sort/#Sort
// Copyright 2009 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found at http://golang.org/LICENSE.

// EtappeSlice is a slice of type Etappe. Use it where you would use []Etappe.
type EtappeSlice []Etappe

// SortBy returns a new ordered EtappeSlice, determined by a func defining ‘less’. See: http://clipperhouse.github.io/gen/#SortBy
func (rcv EtappeSlice) SortBy(less func(Etappe, Etappe) bool) EtappeSlice {
	result := make(EtappeSlice, len(rcv))
	copy(result, rcv)
	// Switch to heapsort if depth of 2*ceil(lg(n+1)) is reached.
	n := len(result)
	maxDepth := 0
	for i := n; i > 0; i >>= 1 {
		maxDepth++
	}
	maxDepth *= 2
	quickSortEtappeSlice(result, less, 0, n, maxDepth)
	return result
}

// Where returns a new EtappeSlice whose elements return true for func. See: http://clipperhouse.github.io/gen/#Where
func (rcv EtappeSlice) Where(fn func(Etappe) bool) (result EtappeSlice) {
	for _, v := range rcv {
		if fn(v) {
			result = append(result, v)
		}
	}
	return result
}

// SelectString projects a slice of string from EtappeSlice, typically called a map in other frameworks. See: http://clipperhouse.github.io/gen/#Select
func (rcv EtappeSlice) SelectString(fn func(Etappe) string) (result []string) {
	for _, v := range rcv {
		result = append(result, fn(v))
	}
	return
}

// Any verifies that one or more elements of EtappeSlice return true for the passed func. See: http://clipperhouse.github.io/gen/#Any
func (rcv EtappeSlice) Any(fn func(Etappe) bool) bool {
	for _, v := range rcv {
		if fn(v) {
			return true
		}
	}
	return false
}

// First returns the first element that returns true for the passed func. Returns error if no elements return true. See: http://clipperhouse.github.io/gen/#First
func (rcv EtappeSlice) First(fn func(Etappe) bool) (result Etappe, err error) {
	for _, v := range rcv {
		if fn(v) {
			result = v
			return
		}
	}
	err = errors.New("no EtappeSlice elements return true for passed func")
	return
}

// Sort implementation based on http://golang.org/pkg/sort/#Sort, see top of this file

func swapEtappeSlice(rcv EtappeSlice, a, b int) {
	rcv[a], rcv[b] = rcv[b], rcv[a]
}

// Insertion sort
func insertionSortEtappeSlice(rcv EtappeSlice, less func(Etappe, Etappe) bool, a, b int) {
	for i := a + 1; i < b; i++ {
		for j := i; j > a && less(rcv[j], rcv[j-1]); j-- {
			swapEtappeSlice(rcv, j, j-1)
		}
	}
}

// siftDown implements the heap property on rcv[lo, hi).
// first is an offset into the array where the root of the heap lies.
func siftDownEtappeSlice(rcv EtappeSlice, less func(Etappe, Etappe) bool, lo, hi, first int) {
	root := lo
	for {
		child := 2*root + 1
		if child >= hi {
			break
		}
		if child+1 < hi && less(rcv[first+child], rcv[first+child+1]) {
			child++
		}
		if !less(rcv[first+root], rcv[first+child]) {
			return
		}
		swapEtappeSlice(rcv, first+root, first+child)
		root = child
	}
}

func heapSortEtappeSlice(rcv EtappeSlice, less func(Etappe, Etappe) bool, a, b int) {
	first := a
	lo := 0
	hi := b - a

	// Build heap with greatest element at top.
	for i := (hi - 1) / 2; i >= 0; i-- {
		siftDownEtappeSlice(rcv, less, i, hi, first)
	}

	// Pop elements, largest first, into end of rcv.
	for i := hi - 1; i >= 0; i-- {
		swapEtappeSlice(rcv, first, first+i)
		siftDownEtappeSlice(rcv, less, lo, i, first)
	}
}

// Quicksort, following Bentley and McIlroy,
// Engineering a Sort Function, SP&E November 1993.

// medianOfThree moves the median of the three values rcv[a], rcv[b], rcv[c] into rcv[a].
func medianOfThreeEtappeSlice(rcv EtappeSlice, less func(Etappe, Etappe) bool, a, b, c int) {
	m0 := b
	m1 := a
	m2 := c
	// bubble sort on 3 elements
	if less(rcv[m1], rcv[m0]) {
		swapEtappeSlice(rcv, m1, m0)
	}
	if less(rcv[m2], rcv[m1]) {
		swapEtappeSlice(rcv, m2, m1)
	}
	if less(rcv[m1], rcv[m0]) {
		swapEtappeSlice(rcv, m1, m0)
	}
	// now rcv[m0] <= rcv[m1] <= rcv[m2]
}

func swapRangeEtappeSlice(rcv EtappeSlice, a, b, n int) {
	for i := 0; i < n; i++ {
		swapEtappeSlice(rcv, a+i, b+i)
	}
}

func doPivotEtappeSlice(rcv EtappeSlice, less func(Etappe, Etappe) bool, lo, hi int) (midlo, midhi int) {
	m := lo + (hi-lo)/2 // Written like this to avoid integer overflow.
	if hi-lo > 40 {
		// Tukey's Ninther, median of three medians of three.
		s := (hi - lo) / 8
		medianOfThreeEtappeSlice(rcv, less, lo, lo+s, lo+2*s)
		medianOfThreeEtappeSlice(rcv, less, m, m-s, m+s)
		medianOfThreeEtappeSlice(rcv, less, hi-1, hi-1-s, hi-1-2*s)
	}
	medianOfThreeEtappeSlice(rcv, less, lo, m, hi-1)

	// Invariants are:
	//	rcv[lo] = pivot (set up by ChoosePivot)
	//	rcv[lo <= i < a] = pivot
	//	rcv[a <= i < b] < pivot
	//	rcv[b <= i < c] is unexamined
	//	rcv[c <= i < d] > pivot
	//	rcv[d <= i < hi] = pivot
	//
	// Once b meets c, can swap the "= pivot" sections
	// into the middle of the slice.
	pivot := lo
	a, b, c, d := lo+1, lo+1, hi, hi
	for {
		for b < c {
			if less(rcv[b], rcv[pivot]) { // rcv[b] < pivot
				b++
			} else if !less(rcv[pivot], rcv[b]) { // rcv[b] = pivot
				swapEtappeSlice(rcv, a, b)
				a++
				b++
			} else {
				break
			}
		}
		for b < c {
			if less(rcv[pivot], rcv[c-1]) { // rcv[c-1] > pivot
				c--
			} else if !less(rcv[c-1], rcv[pivot]) { // rcv[c-1] = pivot
				swapEtappeSlice(rcv, c-1, d-1)
				c--
				d--
			} else {
				break
			}
		}
		if b >= c {
			break
		}
		// rcv[b] > pivot; rcv[c-1] < pivot
		swapEtappeSlice(rcv, b, c-1)
		b++
		c--
	}

	min := func(a, b int) int {
		if a < b {
			return a
		}
		return b
	}

	n := min(b-a, a-lo)
	swapRangeEtappeSlice(rcv, lo, b-n, n)

	n = min(hi-d, d-c)
	swapRangeEtappeSlice(rcv, c, hi-n, n)

	return lo + b - a, hi - (d - c)
}

func quickSortEtappeSlice(rcv EtappeSlice, less func(Etappe, Etappe) bool, a, b, maxDepth int) {
	for b-a > 7 {
		if maxDepth == 0 {
			heapSortEtappeSlice(rcv, less, a, b)
			return
		}
		maxDepth--
		mlo, mhi := doPivotEtappeSlice(rcv, less, a, b)
		// Avoiding recursion on the larger subproblem guarantees
		// a stack depth of at most lg(b-a).
		if mlo-a < b-mhi {
			quickSortEtappeSlice(rcv, less, a, mlo, maxDepth)
			a = mhi // i.e., quickSortEtappeSlice(rcv, mhi, b)
		} else {
			quickSortEtappeSlice(rcv, less, mhi, b, maxDepth)
			b = mlo // i.e., quickSortEtappeSlice(rcv, a, mlo)
		}
	}
	if b-a > 1 {
		insertionSortEtappeSlice(rcv, less, a, b)
	}
}
